#!/bin/bash
# A sane combination of all reposets.
#
# author: andreasl

source "${HOME}/.reposets/admin.reposet"
array+=(${repos[@]})
source "${HOME}/.reposets/dev.reposet"
array+=(${repos[@]})
source "${HOME}/.reposets/extra.reposet"
array+=(${repos[@]})

repos=(${array[@]})
