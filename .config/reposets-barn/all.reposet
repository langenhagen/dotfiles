#!/bin/bash
# A sane combination of all reposets.
#
# author: andreasl

source "${HOME}/.config/reposets/admin.reposet"
array+=(${repos[@]})
source "${HOME}/.config/reposets/dev.reposet"
array+=(${repos[@]})
source "${HOME}/.config/reposets/extra.reposet"
array+=(${repos[@]})

repos=(${array[@]})
