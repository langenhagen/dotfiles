#!/bin/bash
# My default bee reposet.
#
# author: andreasl

repos+=(
    "${HOME}/dotfiles:master:origin:master:origin:master"
    "${HOME}/nextcloud-server:master:origin:master:origin:master"
    "${HOME}/scripts:master:origin:master:origin:master"
    "${HOME}/scripts/generic-scripts:master:origin:master:origin:master"
    "${HOME}/scripts/ping-out:master:origin:master:origin:master"
    "${HOME}/scripts/reposet:master:origin:master:origin:master"
    "${HOME}/setup-my-ubuntu:master:origin:master:origin:master"
)
