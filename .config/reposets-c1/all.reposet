#!/bin/bash
# A sane combination of all available reposets.
#
# author: andreasl

source "${HOME}/.config/reposets/c1.my.reposet"
array+=(${repos[@]})
source "${HOME}/.config/reposets/c1.reposet"
array+=(${repos[@]})
source "${HOME}/.config/reposets/my.reposet"
array+=(${repos[@]})

repos=(${array[@]})
