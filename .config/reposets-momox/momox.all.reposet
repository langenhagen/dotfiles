#!/bin/bash
# A sane combination of my job's reposets, containing all my job's related repos.
#
# author: andreasl

source "${HOME}/.config/reposets/momox.my.reposet"
array+=(${repos[@]})
source "${HOME}/.config/reposets/momox.reposet"
array+=(${repos[@]})

repos=(${array[@]})
